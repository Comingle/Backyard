<%= form_for(@sketch_history) do |f| %>
  <% if @sketch_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sketch_history.errors.count, "error") %> prohibited this sketch_history from being saved:</h2>

      <ul>
      <% @sketch_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
